# -----
# PLEASE DO NOT EDIT THIS FILE. EDIT printer.cfg INSTEAD.
# UPDATES WILL RESET THIS FILE. YOUR EDITS WILL BE LOST WHEN UPDATING.
# -----

[gcode_macro _global_var]
variable_target_extruder: 0
variable_retract_amount: 2
variable_extruder_temp:{'probing': 140, 'cleaning': 190, 'loading': 230, 'purging': 250}
variable_fan_state:{'p1': 0.0, 'p2': 0.0, 'p3': 0.0}
variable_z_maximum_lifting_distance: 256
variable_pause_resume_travel_speed: 150
variable_z_bed_level: -2
gcode:

[gcode_macro PRINT_START]
gcode:
    {% set target_bed = params.BED|int %}
    {% set target_extruder = params.EXTRUDER|int %}
    {% set target_chamber = params.CHAMBER|int %}
    {% set tool = params.TOOL|default(0)|int %}
    {% set sensor = printer['filament_switch_sensor filament_sensor'] if 'filament_switch_sensor filament_sensor' in printer else none %}
    {% set global = printer['gcode_macro _global_var'] %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set heatsoak = settings.heatsoak|default(1)|float %}
    {% set adaptive_mesh = settings.adaptive_mesh|default('false')|lower == 'true' %}
    {% set adaptive_purge = settings.adaptive_purge|default('true')|lower == 'true' %}
    {% set nozzle_z_homing = settings.nozzle_z_homing|default('false')|lower == 'true' %}
    {% set toolhead_led = settings.toolhead_led|default('false')|lower == 'true' %}
    {% set all_calibrated = printer["gcode_macro _CHECK_CALIBRATION_VARS"].all_calibrated %}

    {% if not all_calibrated %}  
        SET_DISPLAY_TEXT MSG="Calibration process not completed. Please complete initial calibration first"  
        CANCEL_PRINT
        RETURN
    {% endif %}

    {% if "AFC" in printer and "TOOL" not in params %}
        RESPOND TYPE=error MSG="Multicolor is only properly supported when using the COSMOS AFC slicer profile. Please slice future prints with the correct profile."
    {% endif %}

    SET_GCODE_VARIABLE MACRO=_global_var VARIABLE=target_extruder VALUE={target_extruder}

    {% if sensor and sensor.enabled and not sensor.filament_detected %}
        SET_DISPLAY_TEXT MSG="NO FILAMENT LOADED!"
        CANCEL_PRINT
        RETURN
    {% endif %}

    {% if toolhead_led %}
        SET_LED LED=hotend WHITE=1
    {% endif %}

    TURN_OFF_FANS
    BED_MESH_CLEAR
    CLEAR_PAUSE
    G90
    M140 S{target_bed}
    M104 S{global.extruder_temp.probing}
    G28

    {% if nozzle_z_homing %}
        CLEAN_NOZZLE # Nozzle cleaning is not needed if we aren't homing via probe
    {% endif %} 
  
    {% if target_chamber > 0 %}
        M191 S{target_chamber}
    {% endif %}
    M190 S{target_bed}

  # rehome after thermal expansion
    SET_DISPLAY_TEXT MSG="Heatsoak: {heatsoak}m"
    G4 P{(heatsoak * 60 * 1000)|int}

    {% if nozzle_z_homing %}
        LOADCELL_Z_HOME
    {% endif %}

    {% if adaptive_mesh %}
        {% if not nozzle_z_homing %}
            SET_DISPLAY_TEXT MSG="Adaptive bed mesh requires nozzle z homing. Please enable nozzle z homing."
            CANCEL_PRINT
            RETURN
        {% endif %}
    
        M109 S{global.extruder_temp.probing}
        SET_DISPLAY_TEXT MSG="Creating adaptive bed mesh"
        SET_GCODE_OFFSET Z=0
        BED_MESH_CALIBRATE_BASE ADAPTIVE=1
        G90
        G1 Z5 F3000 # Move bed a bit down to avoid collision with the nozzle
    {% else %}
        {% if "default" in printer.bed_mesh.profiles %}
            SET_DISPLAY_TEXT MSG="Using default bed mesh"
            BED_MESH_PROFILE LOAD=default
        {% else %}
            SET_DISPLAY_TEXT MSG="NO BED MESH!"
            CANCEL_PRINT
            RETURN
        {% endif %}
    {% endif %}

    {% if "AFC" in printer %}
        T{tool}
    {% endif %}

    M104 S{target_extruder}

    {% if adaptive_purge %}
        SMART_PARK
    {% endif %}

    M109 S{target_extruder}

    {% if adaptive_purge %}
        LINE_PURGE
    {% endif %}
    SET_TEMPERATURE_FAN_TARGET temperature_fan=mainboard TARGET=30
    LOAD_SKEW_IF_EXISTS
    SET_DISPLAY_TEXT ; Clear display text after print start
    G90

[gcode_macro M191]
gcode:
    {% set target_chamber = params.S|int %}

    {% if target_chamber <= 0 %} # Disable heater
        # Default chamber preheat temperature implementation does not need anything to disable
        RETURN
    {% endif %}

    {% if "xyz" not in printer.toolhead.homed_axes %}
        G28
    {% endif %}

    SET_DISPLAY_TEXT MSG="Chamber: {target_chamber}c"
    M106 S255
    G0 X128 Y128 Z10 F9000
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={target_chamber}
    M107

[gcode_macro PRINT_END]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set z_max = global.z_maximum_lifting_distance|int %}
    {% set unload_after_print = settings.unload_after_print|default('false')|lower == 'true' %}
    {% set z_ideal = settings.z_ideal_lifting_distance|default(170)|int %}
    {% set toolhead_led = settings.toolhead_led|default('false')|lower == 'true' %}
    {% set e_mintemp  = printer.configfile.settings['extruder'].min_extrude_temp %}
    {% set sensor = printer['filament_switch_sensor filament_sensor'] if 'filament_switch_sensor filament_sensor' in printer else printer["filament_switch_sensor extruder_tool_start"] %}

    CLEAR_SKEW_IF_EXISTS
  
    {% if printer.toolhead.homed_axes|lower == "xyz" %}
        G91
        {% if sensor.enabled and sensor.filament_detected %}
            {% if (printer.extruder.target != 0 and (printer.extruder.temperature + 5) >= printer.extruder.target and printer.extruder.temperature >= e_mintemp) %}
                M83
                G1 E-5 F600 # TODO: Decide if retract at the end of a print should be configurable and if so, have a global variable for it.

                {% set x = -2 if printer.gcode_move.position.x > 128 else 2 %}
                {% set y = -2 if printer.gcode_move.position.y > 128 else 2 %}

                G1 X{x} Y{y} E-2 F6000
            {% endif %}
        {% endif %}

        {% if ((printer.gcode_move.position.z + 10) < z_ideal) %}
            G1 Z+{(z_ideal - printer.gcode_move.position.z)} F3000
        {% elif (printer.gcode_move.position.z + 10) < z_max %}
            G1 Z+10 F3000
        {% else %}
            G1 Z+{(z_max - printer.gcode_move.position.z)} F3000
        {% endif %}

        {% if sensor.enabled and sensor.filament_detected %} # TODO: This is wrong. We should assume there's filament if the filament sensor is disabled
            {% if (printer.extruder.target != 0 and (printer.extruder.temperature + 5) >= printer.extruder.target and printer.extruder.temperature >= e_mintemp) %}
                {% if unload_after_print %}
                    UNLOAD_FILAMENT
                {% endif %}
            {% endif %}
        {% endif %}

        MOVE_TO_TRAY
    {% endif %}
    TURN_OFF_FANS
    TURN_OFF_HEATERS 
    M220 S100
    M221 S100
    CLEAR_PAUSE
    M84

    {% if toolhead_led %}
        SET_LED LED=hotend WHITE=0
    {% endif %}
    
    RESET_VELOCITY_LIMIT
    SET_GCODE_VARIABLE MACRO=_global_var VARIABLE=target_extruder VALUE=0

[gcode_macro CANCEL_PRINT]
description: Cancel active print and park toolhead
rename_existing: CANCEL_PRINT_BASE
gcode:
    CANCEL_PRINT_BASE
    PRINT_END

[gcode_macro PAUSE]
rename_existing: PAUSE_BASE
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set state = params.STATE|default('normal') %}
    {% set emergency = params.EMERGENCY|default('false')|lower == 'true' %}
    {% set retract_amount = global.retract_amount|float %}
    {% set z_lift_max = global.z_maximum_lifting_distance|int %}
    {% set sensor = printer['filament_switch_sensor filament_sensor'] if 'filament_switch_sensor filament_sensor' in printer else printer["filament_switch_sensor extruder_tool_start"] %}
    {% set pos = printer.gcode_move.position %}

    {% if printer.pause_resume.is_paused %}
        RESPOND MSG="Printer already paused"
        RETURN
    {% endif %}  

    SET_DISPLAY_TEXT MSG="Pausing"
	
	#while paused, we may CUT_FILAMENT, MOVE_TO_TRAY, rehome, or any other number of near boundry movements
	CLEAR_SKEW_IF_EXISTS
	 
    _SAVE_FAN_STATE
    PAUSE_BASE
    TURN_OFF_FANS

    {% if ((printer.extruder.temperature + 5) >= printer.extruder.target) and printer.extruder.can_extrude and sensor.enabled and sensor.filament_detected  %}
        M83
        G1 E-{retract_amount} F300
    {% endif %}

    {% if printer.toolhead.homed_axes|lower == "xyz" %}
        {% set zpos = printer.gcode_move.position.z %}
        G91
        {% if (zpos + 5) < z_lift_max %}
            G1 Z+5 F3000
        {% else %}
            G1 Z+{(z_lift_max - zpos)} F3000
        {% endif %}
        G90
    {% endif %}

    {% if emergency %}
        SET_DISPLAY_TEXT MSG="Emergency pause. Please check the printer and resume when ready."
        M104 S{global.extruder_temp.probing}
        RETURN
    {% endif %}

    {% if state == 'filament_change' %}
        SET_DISPLAY_TEXT MSG="Paused for filament change"
        UNLOAD_FILAMENT
        LOAD_FILAMENT EXTRUDER_TEMP={printer.extruder.target}
    {% elif state == 'runout' %}
        # Runout pause: keep heater on and prompt user to insert filament
        RESPOND MSG="Paused for runout"
        PURGE
        LOAD_FILAMENT EXTRUDER_TEMP={printer.extruder.target}
    {% else %}
        # Keep heater on so resume can prime quickly
        M104 S{global.extruder_temp.probing}
        MOVE_TO_TRAY
    {% endif %}

[gcode_macro RESUME]
rename_existing: RESUME_BASE
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set sensor = printer['filament_switch_sensor filament_sensor'] if 'filament_switch_sensor filament_sensor' in printer else printer["filament_switch_sensor extruder_tool_start"] %}
    {% set extruder_target_temp = printer['gcode_macro _global_var'].target_extruder %}

    # If filament sensor is enabled require filament present before resuming
    {% if sensor.enabled and not sensor.filament_detected %}
        RESPOND TYPE=command MSG="Please insert filament before resuming."
        RETURN
    {% endif %}
    SET_DISPLAY_TEXT
    # Ensure nozzle heated to target before attempting to prime/extrude
    MOVE_TO_TRAY
    M109 S{extruder_target_temp}
    M83
    G1 E50 F300 ; Extrude 50mm to prime. FIXME: This value might require some tweaking. Maybe make configurable.
    KICK
    _RESTORE_FAN_STATE

    {% if "AFC" in printer %}
        M400
        SAVE_GCODE_STATE NAME=PAUSE_STATE
    {% endif %}
	
	# reload SKEW which was cleared at start of PAUSE.  If the skew was purposfully or accidentally loaded by some other mechanism, there is no harm in doing it again
	LOAD_SKEW_IF_EXISTS

    RESUME_BASE


[gcode_macro PURGE]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    _CG28 AXIS=XY
    # Move to purge position
    MOVE_TO_TRAY

    {% if printer.extruder.target < 180 or not printer.extruder.can_extrude %}
        ; Heat nozzle to 250°C and wait
        M104 S{global.extruder_temp.purging} ; Heat nozzle to purging temp
        TEMPERATURE_WAIT SENSOR=extruder MINIMUM={global.extruder_temp.purging} MAXIMUM={global.extruder_temp.purging+5}
    {% endif %}

    # Purge filament (8 cycles)
    G92 E0                          ; Reset extruder position
    M106 S0                       ; Turn off part cooling fan
    {% for purge in range(4) %}
        G1 E25 F300                     ; Extrude 13mm at medium speed
        G1 E10 F150                      ; Extrude 2mm slowly (ensure flow)
        M400                            ; Wait for completion
    {% endfor %}

    KICK
    MOVE_TO_TRAY

[gcode_macro CLEAN_NOZZLE]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set toolhead_target = printer.extruder.target|int %}
    BED_MESH_CLEAR
    M104 S{global.extruder_temp.cleaning}
    _CG28 AXIS=XY
    G90

    # Move to tray
    MOVE_TO_TRAY    
    M104 S{global.extruder_temp.cleaning}
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM={global.extruder_temp.cleaning} MAXIMUM={global.extruder_temp.cleaning+5}
    M104 S{global.extruder_temp.probing}
    M106 S255
    # Wipe
    G1 F9000
    {% for wipes in range(4) %}
        {% for i in range(8) %}
            {% set x = 187 - (i * 2) %}
            {% set y = 264 if i % 2 == 0 else 262 %}
            G1 X{x} Y{y}
        {% endfor %}
        G1 X187
    {% endfor %}
    # Move a 2mm on Y
    G1 Y263
    TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={global.extruder_temp.probing} 

    G1 F9000
    {% for wipes in range(4) %}
        {% for i in range(8) %}
            {% set x = 187 - (i * 2) %}
            {% set y = 262 if i % 2 == 0 else 264 %}
            G1 X{x} Y{y}
        {% endfor %}
        G1 X187
    {% endfor %}
    # Add some clearance
    G1 Y250
    # Clear command queue
    M104 S{toolhead_target}
    M107 
    M400
    SET_DISPLAY_TEXT

[gcode_macro MOVE_TO_TRAY]
gcode:
    _CG28 AXIS=XY

    {% if printer.gcode_move.position.x > 201.5 and printer.gcode_move.position.x < 202.5 and printer.gcode_move.position.y > 263.5 and printer.gcode_move.position.y < 264.5 and "xy" in printer.toolhead.homed_axes %}
        RETURN ; Already at the tray
    {% endif %}

    G90                             ; Absolute positioning
    G0 X202 F12000
    G0 Y250 F12000             ; Move near to the try
    G0 Y264 F1200                 ; Move slowly back to the tray

    M400                            ; Wait for movements to complete

[gcode_macro KICK]
variable_end_kick_x: 186
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set self = printer["gcode_macro KICK"] %}
    {% set end_kick_x = self.end_kick_x|float %}
    {% set retract_amount = global.retract_amount|float %}

    {% if "xyz" not in printer.toolhead.homed_axes %}
        {action_raise_error("Printer not homed")}
    {% endif %}

    {% if printer.extruder.target < 180 or not printer.extruder.can_extrude %}
        {action_raise_error("Extruder not hot enough to kick")}
    {% endif %}

    G90 ; Absolute positioning
    M83 ; E relative

    MOVE_TO_TRAY ; Move to the tray
    G1 E-{retract_amount} F300 ; Retract slightly to reduce pressure during kick
    M106 S255 ; Full blast fan to solidify plastic
    G4 P3000 ; Wait 3 seconds for fan to do its work
    G1 X{self.end_kick_x} F8000 ; Kick left
    M106 S0 ; Turn off fan

    G1 Y250 F6000 ; Exit

    M400 ; Wait for movements to complete

[gcode_macro _LOAD_FILAMENT_STEP_PUSH]
gcode:
    RESPOND TYPE=command MSG="action:prompt_begin Load Filament"
    RESPOND TYPE=command MSG="action:prompt_text Loading filament. Please wait..."
    RESPOND TYPE=command MSG="action:prompt_show"
    {% if printer.extruder.target < 180 or (printer.extruder.temperature + 5 < printer.extruder.target) or not printer.extruder.can_extrude %}
        {action_raise_error("Extruder not hot enough to load filament")}
    {% endif %}
    M83                            ; Relative extrusion mode
    G92 E0                         ; Reset extruder position
    G1 E70 F420
    M400

    RESPOND TYPE=command MSG="action:prompt_begin Loading Filament"
    RESPOND TYPE=command MSG="action:prompt_text Load completed. Do you want to purge more?"
    RESPOND TYPE=command MSG="action:prompt_footer_button Purge more|_LOAD_FILAMENT_STEP_PUSH"
    RESPOND TYPE=command MSG="action:prompt_footer_button Done|_LOAD_FILAMENT_STEP_DONE"
    RESPOND TYPE=command MSG="action:prompt_show"

[gcode_macro _LOAD_FILAMENT_STEP_DONE]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set toolhead_led = settings.toolhead_led|default('false')|lower == 'true' %}
    {% if printer.pause_resume.is_paused %}
        M104 S{global.target_extruder}
        RESPOND TYPE=command MSG="action:prompt_begin Finalizing filament load"
        RESPOND TYPE=command MSG="action:prompt_text Filament loaded. Resume when ready."
        RESPOND TYPE=command MSG="action:prompt_show"
        G4 P3000
        SET_DISPLAY_TEXT MSG="Filament loaded. Resume when ready."
        _CLOSE_PROMPT
    {% else %}
        RESPOND TYPE=command MSG="action:prompt_begin Finalizing filament load"
        RESPOND TYPE=command MSG="action:prompt_text Cleaning nozzle and turning off heaters. Please wait."
        RESPOND TYPE=command MSG="action:prompt_show"

        CLEAN_NOZZLE
        M104 S0

        {% if toolhead_led %}
            SET_LED LED=hotend WHITE=0
        {% endif %}

        _CLOSE_PROMPT
    {% endif %}

[gcode_macro LOAD_FILAMENT]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set target = params.EXTRUDER_TEMP | default(global.extruder_temp.loading) | int %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set toolhead_led = settings.toolhead_led|default('false')|lower == 'true' %}
    _CLOSE_PROMPT

    RESPOND TYPE=command MSG="action:prompt_begin Load Filament"
    RESPOND TYPE=command MSG="action:prompt_text Wait for hotend to heat up"
    RESPOND TYPE=command MSG="action:prompt_show"

    {% if toolhead_led %}
        SET_LED LED=hotend WHITE=1
    {% endif %}

    M104 S{global.extruder_temp.probing}

    _CG28 AXIS=XY

    MOVE_TO_TRAY                    ; Move to the tray
    M104 S{target}
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM={target} MAXIMUM={target+5}
    M107                       ; Turn off part cooling fan (full speed)
    M400
    
    RESPOND TYPE=command MSG="action:prompt_begin Load Filament"
    RESPOND TYPE=command MSG="action:prompt_text Insert filament all the way into extruder before continuing"
    RESPOND TYPE=command MSG="action:prompt_footer_button LOAD|_LOAD_FILAMENT_STEP_PUSH"
    RESPOND TYPE=command MSG="action:prompt_footer_button CANCEL|_LOAD_FILAMENT_CANCEL"
    RESPOND TYPE=command MSG="action:prompt_show"

[gcode_macro _LOAD_FILAMENT_CANCEL]
gcode:
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set toolhead_led = settings.toolhead_led|default('false')|lower == 'true' %}
    _CLOSE_PROMPT
    M104 S0
    {% if toolhead_led %}
        SET_LED LED=hotend WHITE=0
    {% endif %}

[gcode_macro UNLOAD_FILAMENT]
gcode:
    _CG28 AXIS=XY
    CUT_FILAMENT                            ; Wait for movements to complete

    MOVE_TO_TRAY
    COLD_EXTRUDE HEATER=extruder ENABLE=1                    ; Move to the tray
    G1 E-30 F600
    COLD_EXTRUDE HEATER=extruder ENABLE=0
    G0 Y250
    M400                            ; Wait for movements to complete

[gcode_macro CUT_FILAMENT]
variable_retract_length: 10
variable_pushback_length: 5
variable_cut_depth: 3.5
gcode:
    {% set self = printer["gcode_macro CUT_FILAMENT"] %}
    {% set global = printer["gcode_macro _global_var"] %}
    {% set z_max = global.z_maximum_lifting_distance|int %}
    {% set retract_length = self.retract_length|float %}
    {% set pushback_length = self.pushback_length|float %}
    {% set cut_depth = self.cut_depth|float %}

    _CG28 AXIS=XY

    G90
    M83 ; E relative

    {% if printer.extruder.target >= 180 and printer.extruder.can_extrude %}
        G1 E-{retract_length} F300 ; Retract filament
    {% endif %}
    
    G0 Y30 F12000    ;preposition before cut
    G0 X255 F12000        ;move to cut start position
    G0 Y{cut_depth} F6000          ;cut
    G0 Y20 F1200          ;retract slowly to clear housing

    {% if printer.extruder.target >= 180 and printer.extruder.can_extrude %}
        MOVE_TO_TRAY
        G1 E{pushback_length} F300 ; Push back filament
        G1 E-{pushback_length} F600 ; Retract from pushback
    {% endif %}

    M400

[homing_override]
gcode:
    BED_MESH_CLEAR
    {% set rawparams = rawparams|default('XYZ', true) %}
    
    {% if 'z' not in printer.toolhead.homed_axes %}
        FORCE_MOVE STEPPER=stepper_z DISTANCE=5 VELOCITY=20 ; 5 up zhop
    {% else %}
        G91
        G0 Z5 F1200 ; 5 up zhop
    {% endif %}

    G90

    {% if 'Y' in rawparams %}
        G28 Y
    {% endif %}
    {% if 'X' in rawparams %}
        G28 X
    {% endif %}
    {% if 'X' in rawparams and 'Y' in rawparams %}
        G28 Y
    {% endif %}
    {% if 'Z' in rawparams %}
        G0 X128 Y128 F6000
        G28 Z
    {% endif %}

    _LOAD_DEFAULT_BED_MESH

[gcode_macro _LOAD_DEFAULT_BED_MESH]
gcode:
    {% if "default" in printer.bed_mesh.profiles and "xyz" in printer.toolhead.homed_axes %}
        BED_MESH_PROFILE LOAD=default
    {% endif %}

[gcode_macro _CG28]
gcode:
    {% set axis = params.AXIS|default('XYZ')|lower %}
    {% if axis not in printer.toolhead.homed_axes %}
        G28 {axis|upper}
    {% endif %}
    
[gcode_macro LOADCELL_Z_HOME]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set toolhead_target = printer.extruder.target|int %}
    
    M104 S{global.extruder_temp.probing}
    _CG28
    BED_MESH_CLEAR
    M109 S{global.extruder_temp.probing}

    G91
    G0 Z5 F600 ; Move down a Z to get some clearance

    {% for _ in range(2) %}
      G90
      G1 X120 Y-1 F9000
      G4 P500
      PROBE HOME=Z SAMPLES=3 ; Cleaning passes
      G91
      G0 X4 F600
      G0 Z0.8 F300 ; Using pressure of bending the build plate to squish filament
      {% for _ in range(5) %}
        G0 X2 F9000
        G0 Z-1 F300
        G4 P300
        G0 Z1 F300
      {% endfor %}
      G0 Z4 F600
    {% endfor %}

    G91
    G0 Z4 F600 ; Move down a Z to get some clearance
    G90
    G1 X128 Y128 F9000
    G4 P500
    PROBE HOME=Z SAMPLES=3 ; Pass 3, final pass

    G91 
    G0 Z5 F600                       
    G90

    M104 S{toolhead_target} ; Restore temp

[gcode_macro _SAVE_Z_OFFSET]
gcode:
  {% set probe_z_offset = printer.configfile.settings["load_cell_probe"].z_offset %}
  {% set z_offset = ((printer.probe.last_z_result | float) - probe_z_offset) | round(7) %}
  RESPOND MSG="Z offset is off by {z_offset}"
  SET_GCODE_OFFSET Z_ADJUST={z_offset}
  Z_OFFSET_APPLY_ENDSTOP

[gcode_macro Z_OFFSET_APPLY_ENDSTOP]
rename_existing: Z_OFFSET_APPLY_ENDSTOP_BASE
gcode:
  {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
  {% set nozzle_z_homing = settings.nozzle_z_homing|default('false')|lower == 'true' %}

  {% if nozzle_z_homing %}
    { action_raise_error("Z_OFFSET_APPLY_ENDSTOP is not supported when nozzle z homing is enabled.") }
  {% else %}
    Z_OFFSET_APPLY_ENDSTOP_BASE {rawparams}
  {% endif %}

[gcode_macro CALIBRATE_Z_OFFSET]
gcode:
    _CG28

    G91
    G0 Z20 F600
    G28 Z ; Home Z with optical
    M400
    SET_GCODE_OFFSET Z=0 ; imortant to avoid error stacking up on several retries
    BED_MESH_CLEAR
    G90
    G1 X128 Y128 F9000
    PROBE HOME=Z SAMPLES=3 ; Pass 3, final pass
    M400
    _SAVE_Z_OFFSET
    G91
    G0 Z5 F600

[gcode_macro BED_MESH_CALIBRATE]
rename_existing: BED_MESH_CALIBRATE_BASE
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set bed_temp = params.BED_TEMP|default(printer.heater_bed.target if printer.heater_bed.target > 0 else 60)|float %}
    {% set nozzle_z_homing = settings.nozzle_z_homing|default('false')|lower == 'true' %}
    SET_GCODE_OFFSET Z=0
    M140 S{bed_temp}
    G28
    CLEAN_NOZZLE
    M190 S{bed_temp}
    M104 S{global.extruder_temp.probing}
    G4 P60000
    LOADCELL_Z_HOME 

    M109 S{global.extruder_temp.probing}
    TURN_OFF_FANS
    BED_MESH_CALIBRATE_BASE {rawparams}

    {% if not nozzle_z_homing %}
        CALIBRATE_Z_OFFSET
    {% endif %}

[gcode_macro MANUAL_LEVELING]
gcode:
    {% set global = printer['gcode_macro _global_var'] %}
    {% set bed_temp = params.BED_TEMP|default(60)|int %}
    {% set calibration = params.CALIBRATION|default(false)|lower == "true" %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set heatsoak = settings.heatsoak|default(1)|float %}

    M140 S{bed_temp}
    M104 S{global.extruder_temp.probing}

    {% if "xyz" not in printer.toolhead.homed_axes %}
        G28                         ; Home if necessary
    {% endif %}

    BED_MESH_CLEAR
    M400
    {% if calibration %}
        TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={bed_temp-2} MAXIMUM={bed_temp+5}
    {% else %}
        CLEAN_NOZZLE
        TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={bed_temp-2} MAXIMUM={bed_temp+5}
        SET_DISPLAY_TEXT MSG="Heatsoak: {heatsoak}m"
        G4 P{(heatsoak * 60 * 1000)|int}
    {% endif %}

    SCREWS_TILT_CALCULATE {rawparams}

[gcode_macro SCREWS_TILT_CALCULATE]
rename_existing: SCREWS_TILT_CALCULATE_BASE
gcode:
    {% set calibration = params.CALIBRATION|default(false)|lower == "true" %}
    {% set bed_temp = params.BED_TEMP|default(60)|int %}

    _CLOSE_PROMPT
    LOAD_CELL_CALIBRATE TARE=TRUE SAVE=TRUE
    SCREWS_TILT_CALCULATE_BASE
    G1 X128 Y220 Z20 F6000

    _SCREWS_TILT_PROMPT {rawparams}

[gcode_macro _SCREWS_TILT_PROMPT]
gcode:
    {% set stc = printer['screws_tilt_adjust'] %}
    {% set calibration = params.CALIBRATION|default(false)|lower == "true" %}
    {% set global = printer['gcode_macro _global_var'] %}
    {% set bed_z = global.z_bed_level|float %}
    {% set settings = printer['gcode_macro _COSMOS_SETTINGS']|default({}) %}
    {% set endstop_z = printer.configfile.settings['stepper_z'].position_endstop|default(4)|float %}
    {% set target_z = bed_z + endstop_z - 4 %}
    {% set bed_temp = params.BED_TEMP|default(60)|int %}

    {% set pitch = 0.7 %}
    {% set cw_raises_bed = false %}

    {% set s1 = stc.results.screw1 %}{% set d1 = s1.z - target_z %}
    {% set s2 = stc.results.screw2 %}{% set d2 = s2.z - target_z %}
    {% set s3 = stc.results.screw3 %}{% set d3 = s3.z - target_z %}
    {% set s4 = stc.results.screw4 %}{% set d4 = s4.z - target_z %}

    {%- macro adjust(diff) -%}
        {%- set need_raise = diff < 0 -%}
        {%- set dir = "CW" if (need_raise == cw_raises_bed) else "CCW" -%}
        {%- set turns = (diff|abs) / pitch -%}
        {%- set full = turns|int -%}
        {%- set mins = ((turns - full) * 60)|round|int -%}
        {dir} {"%02d:%02d"|format(full, mins)}
    {%- endmacro -%}

    RESPOND TYPE=command MSG="action:prompt_begin Manual Leveling"
    RESPOND TYPE=command MSG="action:prompt_text Adjust screws from the top while holding the bottom with 2.5mm hex, then retry. 01:20 means 1 full turn and 20 minutes, CW=clockwise, CCW=counter-clockwise."
    RESPOND TYPE=command MSG="action:prompt_text Front right screw : adjust {adjust(d1)}, z = {s1.z|round(3)}"
    RESPOND TYPE=command MSG="action:prompt_text Rear right screw : adjust {adjust(d2)}, z = {s2.z|round(3)}"
    RESPOND TYPE=command MSG="action:prompt_text Rear left screw : adjust {adjust(d3)}, z = {s3.z|round(3)}"
    RESPOND TYPE=command MSG="action:prompt_text Front left screw : adjust {adjust(d4)}, z = {s4.z|round(3)}"
    {% if calibration %}
        RESPOND TYPE=command MSG="action:prompt_footer_button Done|_CLOSE_PROMPT"
    {% else %}
        RESPOND TYPE=command MSG="action:prompt_footer_button Done|_SCREWS_TILT_MESH BED_TEMP={bed_temp}"
    {% endif %}
    RESPOND TYPE=command MSG="action:prompt_footer_button Retry|SCREWS_TILT_CALCULATE {rawparams}|warning"
    RESPOND TYPE=command MSG="action:prompt_show"

[gcode_macro _SCREWS_TILT_MESH]
gcode:
    {% set bed_temp = params.BED_TEMP|default(60)|int %}

    _CLOSE_PROMPT
    LOAD_CELL_CALIBRATE TARE=TRUE SAVE=TRUE

    RESPOND TYPE=command MSG="action:prompt_begin Done Leveling"
    RESPOND TYPE=command MSG="action:prompt_text With the bed now manually leveled, the printer needs to make a new bed mesh in order to print correctly."
    RESPOND TYPE=command MSG="action:prompt_footer_button Skip|_SCREWS_TILT_CLOSE"
    RESPOND TYPE=command MSG="action:prompt_footer_button Bed Mesh|_SCREWS_TILT_CLOSE BED_TEMP={bed_temp} MESH=TRUE|warning"
    RESPOND TYPE=command MSG="action:prompt_show"

[gcode_macro _SCREWS_TILT_CLOSE]
gcode:
    {% set bed_temp = params.BED_TEMP|default(60)|int %}
    {% set mesh = params.MESH|default(false)|lower == "true" %}

    _CLOSE_PROMPT

    {% if mesh %}
        SET_DISPLAY_TEXT MSG="Performing probe z-offset & bed mesh calibration"
        BED_MESH_CALIBRATE BED_TEMP={bed_temp}
    {% endif %}
    TURN_OFF_HEATERS
    _SAVE_CONFIG_PROMPT

[gcode_macro _IDLE_TIMEOUT]
gcode:
    {% if printer.print_stats.state == "paused" %}
        RESPOND MSG="PAUSED"
    {% else %}
        M84
        TURN_OFF_HEATERS
        TURN_OFF_FANS
        RESPOND MSG="Idle timeout reached, turning off heaters, steppers and fans!"
    {% endif %}

[gcode_macro TURN_OFF_FANS]
gcode:
    M107
    SET_FAN_SPEED FAN=aux_fan SPEED=0
    SET_FAN_SPEED FAN=case_fan SPEED=0
    SET_TEMPERATURE_FAN_TARGET temperature_fan=mainboard
    RESPOND MSG="ALL FANS OFF!"

[gcode_macro _SAVE_FAN_STATE]
gcode:
    {% set fan_state = {
        'p1': printer.fan.speed|float,
        'p2': printer['fan_generic aux_fan'].speed|float,
        'p3': printer['fan_generic case_fan'].speed|float
    } %}

    SET_GCODE_VARIABLE MACRO=_global_var VARIABLE=fan_state VALUE="{fan_state}"

[gcode_macro _RESTORE_FAN_STATE]
gcode:
    {% set fan_state = printer['gcode_macro _global_var'].fan_state %}

    M106 P1 S{(fan_state.p1|float * 255)|int}
    M106 P2 S{(fan_state.p2|float * 255)|int}
    M106 P3 S{(fan_state.p3|float * 255)|int}

[gcode_macro M600]
gcode:
    PAUSE STATE=filament_change

[gcode_macro M106]
rename_existing: M006
gcode:
  {% set index = params.P|default(1)|int %}
  {% set speed = params.S|default(255)|int %}
  {% set nspeed = speed/255 %}

  {% if index == 1 %} # Part cooling fan
    M006 S{speed}
  {% elif index == 2 %} # Aux cooling fan
    SET_FAN_SPEED FAN=aux_fan SPEED={nspeed}
  {% elif index == 3 %} # Exhaust fan
    SET_FAN_SPEED FAN=case_fan SPEED={nspeed}
  {% else %}
    {action_raise_error("Invalid fan index")}
  {% endif %}

[gcode_macro M107]
rename_existing: M007
gcode:
  {% set index = params.P|default(1)|int %}
  M106 P{index} S0


[gcode_macro _SHOW_PROMPT]
gcode:
    {% set title = params.TITLE|default('Please wait') %}
    {% set message = params.MESSAGE %}
    {% set esc_title = title|replace('"','\\"') %}
    {% set esc_message = message|replace('"','\\"') %}

    RESPOND TYPE=command MSG="action:prompt_begin {esc_title}"
    RESPOND TYPE=command MSG="action:prompt_text {esc_message}"

    # Yes this is a hack. I don't want to hear about it.
    {% if title == "Update Available" %}
        RESPOND TYPE=command MSG="action:prompt_footer_button Update Now|_UPDATE_COSMOS|warning"
    {% endif %}

    RESPOND TYPE=command MSG="action:prompt_footer_button Dismiss|RESPOND TYPE=command MSG=action:prompt_end"

    RESPOND TYPE=command MSG="action:prompt_show"

[gcode_macro _CLOSE_PROMPT]
gcode:
  RESPOND TYPE=command MSG="action:prompt_end"

[gcode_macro SET_DISPLAY_TEXT]
rename_existing: SET_DISPLAY_TEXT_BASE
gcode:
    {% set message = params.MSG|default('') %}

    {% if message %}
        {action_respond_info(message)}
    {% endif %}

    SET_DISPLAY_TEXT_BASE MSG="{message}"


[gcode_macro M729]
gcode:
    { action_emergency_stop("Use COSMOS OrcaSlicer profile (or COSMOS start/end machine gcode).") }

[gcode_macro M8213]
gcode:
    { action_emergency_stop("Use COSMOS OrcaSlicer profile (or COSMOS start/end machine gcode).") }

[gcode_macro LOAD_SKEW_IF_EXISTS]
gcode:
  {% set profile = params.PROFILE|default('my_skew_profile') %}
  {% set section = "skew_correction " ~ profile %}

  # Check if the section '[skew_correction your_profile_name]' exists in the config
  {% if section in printer.configfile.config %}
    RESPOND MSG="Skew profile '{profile}' found. Loading..."
    SKEW_PROFILE LOAD={profile}
  {% else %}
    RESPOND PREFIX="Info:"  MSG="No skew profile defined. If skew correction is desired, create '{profile}' in printer.cfg"
  {% endif %}

[gcode_macro CLEAR_SKEW_IF_EXISTS]
gcode:
  {% set profile = params.PROFILE|default('my_skew_profile') %}
  {% set section = "skew_correction " ~ profile %}

  # Only clear if the profile is actively configured or exists
  {% if section in printer.configfile.config %}
    RESPOND MSG="Clearing Skew Profile"
    SET_SKEW CLEAR=1
  {% endif %}
